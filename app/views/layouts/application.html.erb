<!DOCTYPE html>
<html>
  <head>
    <%= render "layouts/shared/header" %>
  </head>
  <% body_class = @dark_theme ? 'bg-secondary' : 'bg-light' %>
  <body class="<%= body_class %>">
    <%= render "components/navbar" %>
    <%= yield %>
  </body>
</html>
